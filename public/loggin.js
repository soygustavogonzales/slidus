/*

	var a = $("a.btn"),
	btn = $("button.btn"),
	username = $("#username"),
	password = $("#password"),
	idPress = $("#idPress");

	btn.on("click",function(e){
		console.log("me hiciste click")
		var obj ={};
		obj.username = username.value;
		obj.password = password.value;
		obj.idPress = idPress.value;
		a.href = "/admin/"+JSON.stringify(obj);
		a.trigger("click");
	})
*/